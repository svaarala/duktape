=proto
void duk_push_nan(duk_context *ctx);

=stack
[ ... ] -> [ ... NaN! ]

=summary
Pushes a <tt>NaN</tt> (not-a-number) to the stack.

=example
duk_push_nan(ctx);

printf("NaN is number: %d\n", duk_is_number(ctx, -1));

=tags
stack
